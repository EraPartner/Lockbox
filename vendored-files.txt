# Files vendored VERBATIM by sync.sh into each managed .devcontainer/ — one per
# line. Shared source of truth so a fourth canonical file is added in ONE place:
# both sync.sh (its VENDORED array) and CI (.github/workflows/ci.yml vendored job)
# read this manifest. Comments (# ...) and blank lines are ignored.
#
# The generated allowlist.txt is NOT listed here — it is regenerated per project
# from base-allowlist.txt + that project's allowlist.extra.txt, not copied verbatim.
init-firewall.sh
squid.conf
launcher-common.sh
